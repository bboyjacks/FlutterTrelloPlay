import 'package:flutter/material.dart';

class TotalTime extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text("41", style: TextStyle(fontSize: 80, fontWeight: FontWeight.bold),);
  }
}